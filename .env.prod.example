# Database
POSTGRES_USER=rfp
POSTGRES_PASSWORD=CHANGE_ME_secure_password_here
POSTGRES_DB=rfp

# Client app port (optional, default 8080)
CLIENT_PORT=8080

# Gemini API (required for discovery)
GEMINI_API_KEY=your_gemini_api_key

# Client app session (required)
SESSION_SECRET=generate_a_random_32_char_string_here

# R2 Storage (optional - for PDF attachments)
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET=rfp-documents

# Email (optional - for password reset)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=

# Logging
LOG_LEVEL=info
